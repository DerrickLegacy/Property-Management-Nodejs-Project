<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <meta content="Glober Masters Property Managers" name="keywords">
    <meta content="Official Global Masters Page" name="description">
    <title>Property Edit Page</title>
</head>
<body>
  <div class="formbold-main-wrapper">
    <div class="formbold-form-wrapper">
      <form action="/edit/submit/<%= result.propertyId %>" method="POST">
        <div class="formbold-form-title">
          <h2 class="">Edit Property</h2>
        </div>

        <div class="formbold-input-flex">
          <div>
            <label for="propertyName" class="formbold-form-label">Property Name</label>
            <input
            value="<%= result.propertyName %>"
            id="propertyName"
              type="text"
              name="propertyName"
              class="formbold-form-input"
            />
          </div>
          <div>
            <label for="type" class="formbold-form-label">Type</label>
            <input
            value="<%= result.type%>"
              type="text"
              id="type"
              name="type"
              class="formbold-form-input"
            />
          </div>
        </div>
        <div class="formbold-mb-3">
          <label for="address" class="formbold-form-label"> Address</label>
          <input type="text" value="<%= result.address %>" name="address" id="address" class="formbold-form-input" />
        </div>
        <div class="formbold-input-flex">
          <div>
            <label for="numberOfBedroom"  class="formbold-form-label">Number Of Bedrooms</label>
            <input
              value="<%= result.numberOfBeds %>"
              type="text" 
              id="numberOfBedroom"
              name="numberOfBedroom"
              class="formbold-form-input"
            />
          </div>
          <div>
            <label for="numberOfRooms" class="formbold-form-label">Number Of Rooms</label>
            <input
              value="<%= result.numberOfRooms %>"
              type="text"
              name="numberOfRooms"
              id="numberOfRooms"
              class="formbold-form-input"
            />
          </div>
        </div>
        <div class="formbold-input-flex">
          <div>
            <label for="image" class="formbold-form-label">image</label>
            <input
              
              value=" <%= result.image %> "
              type="file" 
              id="image"
              name="image"
              class="formbold-form-input"
            />
          </div>
          <div>
            <label for="postDate" class="formbold-form-label">Post Date</label>
            <input type="date" value="<%= result.postDate %>" name="postDate" id="postDate" class="formbold-form-input"/>
          </div>
        </div>

        <div class="formbold-input-flex">
          <div>
            <label for="cost" class="formbold-form-label">Cost</label>
            <input  value="$<%= result.cost %>" type="number" id="cost" name="cost" class="formbold-form-input"/>
          </div>
          <div>
            <label for="costBefore" class="formbold-form-label">Cost Before</label>
            <input  value="$<%= result.costBefore %>" type="number" id="cost" name="cost" class="formbold-form-input"/>
          </div>
          <div>
            <label for="city" class="formbold-form-label">city</label>
            <select id="city" value="<%= result.city %>" name="city" class="form-select">
              <option>Mukono</option>
              <option>Masaka</option>
              <option>Kampala</option>
              <option selected>Hoima</option>
              <option>Fort Portal</option>
              <option>Masindi</option>
              <option>Mbale</option>
              <option>Other..</option>
            </select>
          </div>
          <div>
            <label for="state" class="formbold-form-label">State</label>
            <input type="text" id="state" value="<%= result.state %>" name="state" class="formbold-form-input"/>
          </div>
        </div>

        <div class="formbold-input-flex">
          <div>
            <label for="zipCode" class="formbold-form-label">Zip </label>
            <input
              value="<%= result.zip %>"
              type="number"
              name="zipCode"
              id="zipCode"
              class="formbold-form-input"
            />
          </div>
        </div>

        
        <button class="formbold-btn">Save Edit</button>
      </form>
    </div>
  </div>
  <%- include('partial/footer')%>
</body>
<style>
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
  }
  body {
    font-family: 'Inter', sans-serif;
  }
  .formbold-mb-3 {
    margin-bottom: 15px;
  }
  .formbold-relative {
    position: relative;
  }
 
 
  #supportCheckbox:checked ~ div span {
    opacity: 1;
  }

  .formbold-main-wrapper {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .formbold-form-wrapper {
    margin: 0 auto;
    max-width: 570px;
    width: 100%;
    background: white;
    padding: 40px;
  }

  .formbold-img {
    margin-bottom: 45px;
  }
.formbold-form-title {
    font-weight: 600;
    font-size: 28px;
    line-height: 34px;
    color: #07074d;
    margin-bottom: 20px;
    text-align: center;
  }
  .formbold-input-flex {
    display: flex;
    gap: 20px;
    margin-bottom: 15px;
  }
  .formbold-input-flex > div {
    width: 50%;
  }
  .formbold-form-input {
    width: 100%;
    padding: 8px 15px;
    border-radius: 5px;
    border: 1px solid #dde3ec;
    background: #ffffff;
    font-weight: 500;
    font-size: 14px;
    color: #536387;
    outline: none;
    resize: none;
  }
  .formbold-form-input:focus {
    border-color: #6a64f1;
    box-shadow: 0px 3px 8px rgba(0, 0, 0, 0.05);
  }
  .formbold-form-label {
    color: #536387;
    font-size: 14px;
    line-height: 24px;
    display: block;
    margin-bottom: px;
  }  
  .formbold-checkbox-inner {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 20px;
    height: 20px;
    margin-right: 16px;
    margin-top: 2px;
    border: 0.7px solid #dde3ec;
    border-radius: 3px;
  }

  .formbold-btn {
    font-size: 14px;
    border-radius: 5px;
    padding:10px 20px;
    border: none;
    font-weight: 500;
    background-color: #6a64f1;
    color: white;
    cursor: pointer;
    margin-top: 10px;
  }
  .formbold-btn:hover {
    background-color: aqua;
    box-shadow: 0px 3px 8px rgba(0, 0, 0, 0.05);
  }
</style>
</html>